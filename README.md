# 🚀 FastAPI Microservices - ToDo & URL Shortener

[![Python](https://img.shields.io/badge/Python-3.13-blue.svg)](https://www.python.org/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.128.0-green.svg)](https://fastapi.tiangolo.com/)
[![Docker](https://img.shields.io/badge/Docker-Enabled-blue.svg)](https://www.docker.com/)


> **Автор:** Сычёв Иван Александрович  
> **Год:** 2025  
> **Учебное заведение:** Уральский Федеральный Университет (УрФУ)

---

## 📋 Описание проекта

Два полнофункциональных микросервиса на **FastAPI** с веб-интерфейсами, упакованные в **Docker** контейнеры:

### 1️⃣ **ToDo Service** - Менеджер задач
- ✅ CRUD операции (создание, чтение, обновление, удаление)
- 📝 Красивый веб-интерфейс с формами
- ✔️ Отметка выполненных задач
- 💾 SQLite база данных с сохранением в Docker-томах
- 🎨 Современный дизайн с градиентами и анимациями

### 2️⃣ **URL Shortener** - Сокращатель ссылок с QR-кодами
- 🔗 Создание коротких ссылок из длинных URL
- 🎨 **Градиентный цветовой пикер** для QR-кодов
- 📱 Генерация QR-кодов в любом цвете
- 💾 Скачивание QR-кодов в PNG
- 📊 Статистика переходов по ссылкам
- 🔄 Редирект на оригинальные URL
- 💾 SQLite база данных с сохранением в Docker-томах
- 🎨 Интерактивный веб-интерфейс

---

## 🐳 Docker Hub

Образы опубликованы и готовы к использованию:

- **ToDo Service:** [hub.docker.com/r/ivanmmmlchik/todo-service](https://hub.docker.com/r/ivanmmmlchik/todo-service)
- **URL Shortener:** [hub.docker.com/r/ivanmmmlchik/shorturl-service](https://hub.docker.com/r/ivanmmmlchik/shorturl-service)

---

## 🚀 Быстрый старт

### Запуск через Docker (рекомендуется)
```bash
# 1. Создайте Docker-тома для сохранения данных
docker volume create todo_data
docker volume create shorturl_data

# 2. Запустите ToDo Service
docker run -d -p 8000:80 -v todo_data:/app/data \
  --name todo-container \
  ivanmmmlchik/todo-service:latest

# 3. Запустите URL Shortener
docker run -d -p 8001:80 -v shorturl_data:/app/data \
  --name shorturl-container \
  ivanmmmlchik/shorturl-service:latest

# 4. Проверьте статус контейнеров
docker ps
```

### Доступ к сервисам

| Сервис | API Документация | Веб-интерфейс |
|--------|-----------------|---------------|
| **ToDo Service** | [localhost:8000/docs](http://localhost:8000/docs) | [todo-service/index.html](./todo-service/index.html) |
| **URL Shortener** | [localhost:8001/docs](http://localhost:8001/docs) | [shorturl-service/index.html](./shorturl-service/index.html) |

---

## 💻 Локальная разработка

### Требования
- Python 3.13+
- pip
- virtualenv

### Установка
```bash
# 1. Клонируйте репозиторий
git clone https://github.com/IvanMMMLich/fastapi-microservices.git
cd fastapi-microservices

# 2. Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# 3. Установите зависимости
pip install -r todo-service/requirements.txt
pip install -r shorturl-service/requirements.txt
```

### Запуск сервисов

**ToDo Service:**
```bash
cd todo-service
uvicorn main:app --reload --port 8000
# Доступ: http://localhost:8000/docs
```

**URL Shortener:**
```bash
cd shorturl-service
uvicorn main:app --reload --port 8001
# Доступ: http://localhost:8001/docs
```

---

## 📂 Структура проекта
```
fastapi-microservices/
│
├── todo-service/                 # Сервис управления задачами
│   ├── Dockerfile               # Конфигурация Docker
│   ├── main.py                  # Основной код FastAPI
│   ├── requirements.txt         # Зависимости Python
│   └── index.html              # Веб-интерфейс
│
├── shorturl-service/            # Сервис сокращения ссылок
│   ├── Dockerfile              # Конфигурация Docker
│   ├── main.py                 # Основной код FastAPI
│   ├── requirements.txt        # Зависимости Python
│   └── index.html             # Веб-интерфейс с QR-генератором
│
├── .gitignore                  # Игнорируемые файлы
└── README.md                   # Документация проекта
```

---

## ⚙️ Технологии

### Backend
- **FastAPI** 0.128.0 - современный веб-фреймворк
- **Uvicorn** - ASGI сервер
- **Pydantic** - валидация данных
- **SQLite** - встроенная база данных
- **qrcode** - генерация QR-кодов
- **Pillow** - обработка изображений

### Frontend
- **HTML5 / CSS3** - структура и стилизация
- **JavaScript (Vanilla)** - интерактивность
- **Градиенты CSS** - современный дизайн
- **Canvas API** - работа с цветами

### DevOps
- **Docker** - контейнеризация
- **Docker Volumes** - персистентное хранение данных
- **Docker Hub** - публикация образов

---

## 🎯 Особенности реализации

### ToDo Service

**Эндпоинты API:**
- `POST /items` - Создать задачу
- `GET /items` - Получить все задачи
- `GET /items/{id}` - Получить задачу по ID
- `PUT /items/{id}` - Обновить задачу
- `DELETE /items/{id}` - Удалить задачу

**Веб-интерфейс:**
- Форма добавления задач
- Список с карточками задач
- Чекбоксы для отметки выполнения
- Кнопки редактирования и удаления
- Адаптивный дизайн

### URL Shortener

**Эндпоинты API:**
- `POST /shorten` - Создать короткую ссылку
- `GET /all` - Получить все ссылки
- `GET /stats/{short_id}` - Статистика по ссылке
- `GET /{short_id}` - Редирект на полный URL
- `POST /qrcode` - Генерация QR-кода
- `DELETE /delete/{short_id}` - Удалить ссылку

**Веб-интерфейс:**
- Форма создания коротких ссылок
- **Градиентный цветовой слайдер** (чёрный → красный → зелёный → синий → жёлтый → и т.д.)
- Генерация QR-кодов в выбранном цвете
- Предпросмотр QR-кода
- Скачивание QR-кода в PNG
- Копирование ссылок в буфер обмена
- Статистика переходов
- Удаление ссылок

---

## 🔧 Docker конфигурация

### Создание образов локально
```bash
# Собрать образ ToDo Service
docker build -t ivanmmmlchik/todo-service:latest todo-service/

# Собрать образ URL Shortener
docker build -t ivanmmmlchik/shorturl-service:latest shorturl-service/
```

### Управление контейнерами
```bash
# Остановить контейнеры
docker stop todo-container shorturl-container

# Запустить контейнеры
docker start todo-container shorturl-container

# Удалить контейнеры
docker rm todo-container shorturl-container

# Посмотреть логи
docker logs todo-container
docker logs shorturl-container
```

### Персистентность данных

Данные сохраняются в **Docker Volumes** и не теряются при перезапуске контейнеров:
```bash
# Посмотреть тома
docker volume ls

# Информация о томе
docker volume inspect todo_data

# Удалить том (ОСТОРОЖНО: все данные будут потеряны!)
docker volume rm todo_data
```

---

## 📸 Примеры использования

### ToDo Service
1. Откройте веб-интерфейс `index.html`
2. Введите название задачи: "Купить молоко"
3. Добавьте описание: "В магазине на углу"
4. Нажмите "Добавить задачу"
5. Задача появится в списке с чекбоксом и кнопкой удаления

### URL Shortener
1. Откройте веб-интерфейс `index.html`
2. Введите длинную ссылку: `https://www.youtube.com/watch?v=dQw4w9WgXcQ`
3. Получите короткую: `http://localhost:8001/abc123`
4. Нажмите "Создать QR-код"
5. Выберите цвет на градиентном слайдере (например, ярко-красный)
6. Сгенерируйте и скачайте QR-код
7. Отсканируйте QR-код телефоном - откроется оригинальная ссылка!

---

## 🧪 Тестирование

### Через Swagger UI (автодокументация FastAPI)

**ToDo Service:**
```
http://localhost:8000/docs
```

**URL Shortener:**
```
http://localhost:8001/docs
```

### Через curl
```bash
# Создать задачу
curl -X POST "http://localhost:8000/items" \
  -H "Content-Type: application/json" \
  -d '{"title":"Test Task","description":"Test Description"}'

# Создать короткую ссылку
curl -X POST "http://localhost:8001/shorten" \
  -H "Content-Type: application/json" \
  -d '{"url":"https://github.com"}'
```

---

## 📝 Задание

Данный проект выполнен в рамках учебного задания:

**Требования:**
- ✅ Разработать два микросервиса на FastAPI
- ✅ Реализовать CRUD-операции (ToDo)
- ✅ Реализовать сокращение URL с редиректом
- ✅ Использовать SQLite для хранения данных
- ✅ Упаковать в Docker-контейнеры
- ✅ Использовать именованные Docker-тома
- ✅ Опубликовать на GitHub
- ✅ Опубликовать образы на Docker Hub
- ✅ Создать документацию (README)

**Дополнительные фичи (сверх задания):**
- 🎨 Красивые веб-интерфейсы для обоих сервисов
- 🎨 Градиентный цветовой пикер для QR-кодов
- 📱 Генерация QR-кодов в любом цвете
- 💾 Скачивание QR-кодов
- 📊 Статистика переходов
- 🗑️ Удаление ссылок через UI
- 📋 Копирование ссылок в буфер обмена

---

## 🔗 Полезные ссылки

- **GitHub репозиторий:** [github.com/IvanMMMLich/fastapi-microservices](https://github.com/IvanMMMLich/fastapi-microservices)
- **Docker Hub - ToDo:** [hub.docker.com/r/ivanmmmlchik/todo-service](https://hub.docker.com/r/ivanmmmlchik/todo-service)
- **Docker Hub - URL Shortener:** [hub.docker.com/r/ivanmmmlchik/shorturl-service](https://hub.docker.com/r/ivanmmmlchik/shorturl-service)
- **FastAPI документация:** [fastapi.tiangolo.com](https://fastapi.tiangolo.com/)
- **Docker документация:** [docs.docker.com](https://docs.docker.com/)

---

## 👨‍💻 Автор

**Сычёв Иван Александрович**  
Студент магистратуры  
Уральский Федеральный Университет (УрФУ), Екатеринбург  
Направление: Machine Learning Engineering  

**Контакты:**
- GitHub: [@IvanMMMLich](https://github.com/IvanMMMLich)
- Email: sychovivanivan15@mail.ru
